server:
  port: 8808

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1:3306/boot_crm?characterEncoding=utf8&useSSL=true
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.mypro.domain
